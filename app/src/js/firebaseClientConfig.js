// Configuracao legada: mantida para compatibilidade com imports antigos.
// Fonte oficial agora fica em /app/src/js/config.js.
import { FIREBASE_CLIENT_CONFIG } from "./config.js";

export const firebaseConfig = { ...FIREBASE_CLIENT_CONFIG };
